---
import bottleImage from '../assets/12_atomisador.png';
---

<section class="banner-full-section w-full h-screen min-h-[600px] flex flex-col justify-center items-center bg-[#f6f6f6] relative overflow-x-hidden overflow-y-hidden">
  <div class="absolute top-0 left-0 w-full h-2 bg-[#01ffff]"></div>
  <div class="flex flex-col md:flex-row justify-center items-center w-full h-full max-w-[1400px] mx-auto px-4 z-10">
    <!-- Imagen del frasco -->
    <div class="flex justify-center items-center w-full md:w-1/2 mb-8 md:mb-0">
      <img src={bottleImage.src} alt="Atomisador" class="max-h-[400px] md:max-h-[500px] w-auto object-contain drop-shadow-xl banner-bottle" />
    </div>
    <!-- Texto y botón -->
    <div class="flex flex-col justify-center items-center md:items-start w-full md:w-1/2 text-center md:text-left gap-6">
      <h2 class="font-[Antonio] text-[#464b68] text-[clamp(5rem,7vw,8.4rem)] font-normal leading-none banner-title">LET’S BUILD<br><span class="font-[Antonio] text-[#464b68]">SOMETHING</span></h2>
      <h3 class="font-[Antonio] text-[#015c9f] text-[clamp(2.86rem,4.1vw,4.85rem)] font-extralight leading-none tracking-tight banner-subtitle">EXCEPTIONAL TOGETHER</h3>
      <p class="font-[Montserrat] text-[#222] text-[clamp(1.1rem,2vw,2rem)] max-w-xl mt-2 banner-desc">
        <span class="font-bold">Ready to bring your product </span> to life<br />
        <span>or scale your existing production?</span>
      </p>
      <a href="#contact" class="inline-block mt-2 xl:mt-10 hero-btn-link">
        <button class="bg-[#ffba00] hover:bg-[#daa300] text-black font-[Antonio] font-bold sm:text-sm md:text-lg lg:text-xl xl:text-2xl 2xl:text-4xl px-8 py-3 rounded-md shadow-lg transition-all duration-200 border-none outline-none hero-btn">
          START YOUR PROJECT TODAY
        </button>
      </a>
</section>

<script>
  import { gsap } from 'gsap';
  if (typeof window !== "undefined") {
    const animateBanner = () => {
      const tl = gsap.timeline();
      tl.from('.banner-title', {
        y: 60,
        opacity: 0,
        duration: 0.8,
        ease: 'power3.out'
      });
      tl.from('.banner-subtitle', {
        y: 40,
        opacity: 0,
        duration: 0.7,
        ease: 'power3.out'
      }, '-=0.6');
      tl.from('.banner-desc', {
        y: 30,
        opacity: 0,
        duration: 0.6,
        ease: 'power2.out'
      }, '-=0.5');
      tl.fromTo('.banner-btn',
        { scale: 0.8, opacity: 0 },
        { scale: 1, opacity: 1, duration: 0.5, ease: 'back.out(1.7)' },
        '-=0.4'
      );
      tl.from('.banner-bottle', {
        x: 80,
        opacity: 0,
        duration: 1,
        ease: 'power2.out'
      }, '-=0.7');
    };
    document.addEventListener('DOMContentLoaded', () => {
      const section = document.querySelector('.banner-full-section');
      if (!section) return;
      const observer = new window.IntersectionObserver((entries) => {
        entries.forEach(entry => {
          if (entry.isIntersecting) {
            animateBanner();
          }
        });
      }, { threshold: 0.4 });
      observer.observe(section);
    });
  }
</script>
    </div>
  </div>
</section>

<style>
  .font-antonio {
    font-family: 'Antonio', sans-serif;
    font-weight: 700;
    letter-spacing: 0.01em;
  }
  .font-montserrat {
    font-family: 'Montserrat', sans-serif;
    font-weight: 400;
  }
</style>
